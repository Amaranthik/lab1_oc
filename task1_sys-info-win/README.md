# Задание 1: System Info (WinAPI)

Консольная утилита для получения системной информации операционной системы Windows. 
Программа реализована на чистом **Python** с использованием библиотеки `ctypes` для прямых вызовов функций Windows API, без использования высокоуровневых библиотек (таких как `psutil` или `platform`).

## 📋 Функционал

Программа выводит информацию в следующем порядке:
1.  **Версия ОС**: Реальная версия Windows (аналог проверки `IsWindows10OrGreater`).
2.  **Идентификация**: 
    *   **Имя компьютера**.
    *   **Имя текущего пользователя**.
3.  **Процессор**: 
    *   **Архитектура процессора (x64, x86, ARM)**.
    *   Количество логических ядер.
4.  **Память**: 
    *   Размер физической памяти (RAM).
    *   Размер виртуальной памяти.
    *   **Использование памяти в процентах (Memory Load)**.
5.  **Файл подкачки**: Текущий размер (Commit Charge).
6.  **Диски**: Список логических дисков, файловая система и объёмы.


Программа выводит информацию в следующем порядке:
1.  **Версия ОС**: Реальная версия Windows (аналог проверки `IsWindows10OrGreater`).
2.  **Идентификация**: 
    *   Имя компьютера.
    *   Имя текущего пользователя.
3.  **Архитектура процессора**: Тип архитектуры (x64, x86, ARM).
4.  **Память**: 
    *   Размер физической памяти (RAM).
    *   Размер виртуальной памяти (Virtual Memory).
    *   Использование памяти в процентах (Memory Load).
5.  **Файл подкачки**: Текущее использование / Общий лимит (Commit Charge).
6.  **Процессоры**: Количество логических ядер.
7.  **Диски**: Список логических дисков, файловая система и объёмы.

## 🛠 Техническая реализация

| Требуемые данные | Библиотека | Функция API | Почему выбрана эта функция? |
| :--- | :--- | :--- | :--- |
| **Версия ОС** | `ntdll.dll` | `RtlGetVersion` | Стандартная `GetVersionEx` возвращает недостоверную версию (6.2) на Win 8.1/10. `RtlGetVersion` всегда возвращает точные данные. |
| **Имя компьютера** | `kernel32.dll` | `GetComputerNameW` | Получает NetBIOS-имя локального компьютера. |
| **Имя пользователя** | `advapi32.dll` | `GetUserNameW` | Получает имя пользователя, связанного с текущим потоком. |
| **Архитектура CPU** | `kernel32.dll` | `GetNativeSystemInfo` | Обычная `GetSystemInfo` может вернуть x86, если скрипт запущен в 32-битном режиме. Native-версия показывает реальное железо. |
| **RAM и Memory Load** | `kernel32.dll` | `GlobalMemoryStatusEx` | Возвращает структуру `MEMORYSTATUSEX` с данными о физической памяти и проценте загрузки. |
| **Файл подкачки** | `psapi.dll` | `GetPerformanceInfo` | Возвращает информацию о текущем использовании и лимите памяти (RAM + Файл подкачки). |
| **Количество ядер** | `kernel32.dll` | `GetNativeSystemInfo` | Возвращает количество логических процессоров (выводится отдельным блоком). |
| **Диски** | `kernel32.dll` | `GetLogicalDriveStringsW`<br>`GetDiskFreeSpaceExW` | Перебор букв дисков, определение файловой системы и свободного места. |
## 🚀 Запуск

Программа не требует установки сторонних библиотек.

```bash
py sys_info_win.py